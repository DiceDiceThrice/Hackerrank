Determines if a class is cancelled based on student attendance. Students arriving at time ≤ 0 are on time. The professor cancels class if fewer than k students arrive on time. It counts students with arrival time ≤ 0 and returns "YES" (cancelled) if count < k, otherwise "NO" (class held).